# My Claude Prompt

A **_BLAZINGLY FAST_** claude prompt (that is, faster than my ruby implementation by 0.11 seconds).

Shows caveman mode info, warns on using a model other than sonnett, and warns as usage percentages increase.

Set `CLAUDE_VERBOSE_STATUS` to a truey value (such as `true`) to show all fields, instead of hiding ones that aren't relevant.
